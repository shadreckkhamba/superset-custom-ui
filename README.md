Custom-charts

*** Superset Setup ***
Enable firewall to nginx ports in docker.yml file to allow tcp access connection and resolve the firwall error.


To resolve packages mismatch error 
npm install or npm audit fix --force in superset-frontend

To free up storage to
docker system prune -f 

access superset via localhost:5000 but before run docker compose up --build


